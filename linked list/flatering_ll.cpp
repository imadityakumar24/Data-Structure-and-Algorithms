
#include <iostream>
using namespace std;

struct node {
    int * data;
    node * next;
    node * child;

    node(int x) {
        next = nullptr;
        child = nullptr;
    }
};


node flattern(node * head) {
    node;
}